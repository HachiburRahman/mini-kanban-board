# Read by docker-compose (root .env). Only affects the "backend" service's
# JWT signing secret - see backend/.env.example and frontend/.env.example
# for the variables each app needs when NOT run through Docker.
JWT_SECRET=change-me-to-a-long-random-string
