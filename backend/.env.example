# PostgreSQL connection string (docker-compose default values shown)
DATABASE_URL="postgresql://kanban:kanban@localhost:5432/kanban?schema=public"

# Any long random string works locally; use a real secret in production
JWT_SECRET="replace-with-a-long-random-string"
JWT_EXPIRES_IN="7d"

PORT=4000
